'use client'
import { useEffect, useState } from 'react'
import { useParams, useRouter } from 'next/navigation'
import { supabase } from '@/lib/supabaseBrowser'
const fieldStyle={width:'100%',minHeight:42,padding:'9px 12px',border:'1px solid #aabdb3',borderRadius:8,background:'#fbfdfb'}
export default function EditArtistPage(){const{ id }=useParams<{id:string}>();const router=useRouter();const[artist,setArtist]=useState<any>(null);const[categories,setCategories]=useState<any[]>([]);const[error,setError]=useState('');const[saving,setSaving]=useState(false);useEffect(()=>{Promise.all([supabase.from('artists').select('first_name,last_name,year_of_birth,year_of_death,place_of_birth,place_of_death,notes,artist_category_no').eq('id',id).maybeSingle(),supabase.from('artist_categories').select('legacy_no,description').order('legacy_no')]).then(([a,c])=>{if(a.error||!a.data)setError('Artist not found');else setArtist(a.data);setCategories(c.data??[])})},[id]);async function save(){if(!artist)return;setSaving(true);const{error}=await supabase.from('artists').update(artist).eq('id',id);if(error)setError(error.message);else router.push('/referentials');setSaving(false)}async function remove(){if(!confirm('Delete this artist?'))return;const{error}=await supabase.from('artists').delete().eq('id',id);if(error)setError(error.message);else router.push('/referentials')}if(!artist)return <main style={{padding:96}}>{error||'Loading...'}</main>;const update=(p:any)=>setArtist({...artist,...p});const row=(label:string,control:React.ReactNode)=><label style={{display:'grid',gridTemplateColumns:'160px 1fr',gap:12,marginBottom:12}}>{label}{control}</label>;return <main style={{padding:'96px 20px 56px',minHeight:'100vh',background:'#f3f5f1'}}><section style={{maxWidth:760,margin:'0 auto',padding:30,background:'#fff',border:'1px solid #d7dfda',borderRadius:12,color:'black'}}><h1>Edit artist</h1>{error&&<p style={{color:'#a22'}}>{error}</p>}{row('First name',<input style={fieldStyle} value={artist.first_name??''} onChange={e=>update({first_name:e.target.value})}/>)}{row('Last name',<input style={fieldStyle} value={artist.last_name??''} onChange={e=>update({last_name:e.target.value})}/>)}{row('Year of birth',<input style={fieldStyle} type="number" value={artist.year_of_birth??''} onChange={e=>update({year_of_birth:e.target.value?Number(e.target.value):null})}/>)}{row('Year of death',<input style={fieldStyle} type="number" value={artist.year_of_death??''} onChange={e=>update({year_of_death:e.target.value?Number(e.target.value):null})}/>)}{row('Category',<select style={fieldStyle} value={artist.artist_category_no??''} onChange={e=>update({artist_category_no:e.target.value?Number(e.target.value):null})}><option value="">-</option>{categories.map(c=><option key={c.legacy_no} value={c.legacy_no}>{c.description}</option>)}</select>)}{row('Place of birth',<input style={fieldStyle} value={artist.place_of_birth??''} onChange={e=>update({place_of_birth:e.target.value})}/>)}{row('Place of death',<input style={fieldStyle} value={artist.place_of_death??''} onChange={e=>update({place_of_death:e.target.value})}/>)}{row('Notes',<textarea style={{...fieldStyle,minHeight:110}} value={artist.notes??''} onChange={e=>update({notes:e.target.value})}/>)}<div style={{display:'flex',justifyContent:'flex-end',gap:8}}><button className="edit-button" onClick={()=>router.back()}>Cancel</button><button className="edit-button" onClick={remove}>Delete</button><button className="edit-button" disabled={saving} onClick={save}>{saving?'Saving...':'Save'}</button></div></section></main>}
